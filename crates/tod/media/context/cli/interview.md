## `tod-cli interview`

Interview session state.

```
tod-cli --data-root <DATA_ROOT> interview exhausted --session <UUID> --reason <TEXT>
```

Declare that you have nothing further to ask or process in this session, with
why. The driver reads this to decide whether the interview can complete.
