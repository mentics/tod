[package]
name = "tod"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[[bin]]
name = "tod"
path = "src/main.rs"

[features]
default = ["agent-socket"]
agent-socket = ["tod-ui/agent-socket"]

[dependencies]
tod-ui = { path = "../tod-ui", default-features = false }
