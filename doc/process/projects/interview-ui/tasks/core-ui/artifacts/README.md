# Artifacts — core-ui

Task-scoped durable packages (visual mockups and any future non-visual artifacts).

Linked from `design.md`. Visual co-design sessions write Accepted packages here (prefer `visual/{screen}/preview.png` + optional notes/source/HTML).
