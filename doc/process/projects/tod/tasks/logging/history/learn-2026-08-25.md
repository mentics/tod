# Learn — logging — 2026-08-25

Shipped: `af509d8` on `main` (`tracing-rolling-file`, settings/CLI level, KB size cap, NDJSON under `.local/logs/tod/`).

## Finding (acted on)

Design named a file-appender family without confirming **size-based** rolling support. That caused custom roller → review defects → crate switch. Process: added a `design` → `planning` checklist item to verify named libraries against each required capability (docs or spike).

## Other notes (no process edit)

- Late MB→KB change in planning via Modify (doc churn).
- Long requirements interview; some probes voided by earlier answers.
- Review F2 (settings load hard-fail broader than design) accepted as not critical.

Full write-up: `.local/agent/process/projects/tod/tasks/logging/journal/2026-08-25-1305-learn.md`.
