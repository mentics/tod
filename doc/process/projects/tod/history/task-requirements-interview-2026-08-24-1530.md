# Task requirements interview — tod — 2026-08-24

## Session

**Agent:** Interview UI kickoff — researcher bootstrap for task requirements interview. SQLite session id 1. Display name: tod (project) — Task requirements. Entity: project `tod` (`doc/process/projects/tod`). Phase: task-requirements-interview. Mode: interactive.

**Prior context:**

- Project `tod` is **active** — project intake complete; see `user.md` for cross-cutting requirements.
- Prior tasks: **ui-scaffolding** (done) — runnable GPUI desktop shell; **task-list** (verifying) — task list UI with keyboard navigation and reusable `ListView<T>`.
- `refs/process/todo.md` — next work: Interview UI (queue folder listing, parallel answers, researcher replenishment, two-session model).
- Separate project `interview-ui` defines cross-cutting Interview UI requirements (may port into a tod task).
- Kickoff entity path is at **project** level; task slug and task `user.md` to be established in this interview.

**Scoping to resolve:**

- Which task under `tod` this interview defines
- Requirements breadth and source (interview-ui project vs fresh)
- Agent invocation, verification bar, constraints, and out-of-scope boundaries

---
